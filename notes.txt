the dhi repo has to be at the same level as the scripts files

1. create elastic search index - DONE
2. invoke lambda with events 
3. check lambda

test cases:
bib
item 
holding

sam local invoke --profile nypl-digital-dev -t sam.qa.yml -e ../events/$file