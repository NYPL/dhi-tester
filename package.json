{
  "name": "dhi-test-prototype",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-es": "node -r dotenv/config create-elastic-index.js",
    "install-inner": "cd ./disovery-hybrid-indexer; npm i",
    "send-events": "cd ./discovery-hybrid-indexer; for file in ../events/*.json; do aws lambda invoke --function-name \"DiscoveryHybridIndexer\" --endpoint-url \"http://127.0.0.1:3001\" --payload file://$file out.txt --cli-binary-format raw-in-base64-out; done",
    "start-lambda": "cd ./discovery-hybrid-indexer; sam local start-lambda -t sam.test.yml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1112.0",
    "discovery-api-indexer": "git+https://github.com/NYPL-discovery/discovery-api-indexer.git#v1.0.2",
    "dotenv": "^16.0.0"
  }
}
